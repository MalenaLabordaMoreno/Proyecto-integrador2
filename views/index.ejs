<!DOCTYPE html>
<html>
  <%- include('./partials/head') %>
  <body>
	<% if (user != null) { %>
		<%- include('headerLogueado')
	<% } else { %>
		<%- include('header')
	<% } %>	
    <%- include('./partials/banner') %>
	<!-- Novedades -->
	<div class="container products-wrapper">
    	<div class="row">
        	<div class="col-12">
            	<h2 class="products-title">Novedades</h2>
        	</div>
        	<!-- Show the "ultimos" products -->
        	<% for (let i = 0; i < 4; i++) { %>
            	<div class="col-12 col-sm-6 col-lg-3">
                	<section class="product-box">
                    	<a href="/productos/detalle/<%= lista_productos[i].id %>">
                        	<figure class="product-box_image">
                            	<img src="<%= lista_productos[i].imagen %>" alt="<%= lista_productos[i].nombre %>">
                        	</figure>
                        	<article class="product-box_data">
                            	<h2><%= lista_productos[i].nombre %></h2>
                            	<p><%= lista_productos[i].descripcion %></p>
                            	<p>Comentarios: <%= lista_productos[i].comentarios.length %></p>
                        	</article>
                    	</a>
                	</section>
            	</div>
        	<% } %>
    	</div>
	</div>
	<!-- /Novedades -->
	<!-- Más comentados -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Más comentados</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<% for (let i = 4; i < 8; i++) { %>
			<div class="col-12 col-sm-6 col-lg-3">
				<section class="product-box">
					<a href="/productos/detalle/<%= lista_productos[i].id %>">
						<figure class="product-box_image">
							<img src="<%= lista_productos[i].imagen %>" alt="<%= lista_productos[i].nombre %>">
						</figure>
						<article class="product-box_data">
							<h2><%= lista_productos[i].nombre %></h2>
							<p><%= lista_productos[i].descripcion %></p>
							<p>Comentarios: <%= lista_productos[i].comentarios.length %></p>
						</article>
					</a>
				</section>
			</div>
			<% } %>
		</div>
	</div>
	<!-- /Más comentados -->
    <%- include('./partials/foot')%>
  </body>
</html>
